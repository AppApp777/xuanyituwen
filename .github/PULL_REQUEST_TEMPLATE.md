## 变更目的

- 这个 PR 解决了哪个可复现问题？
- 影响的是故事流程、输出合同、脚本还是文档？

## 证据

- [ ] 已新增或更新 `evals/fixtures/` 与机器断言。
- [ ] 已运行 `python scripts/validate_evals.py`。
- [ ] 已运行 `python scripts/validate_artifact_package.py examples/cat-eye-person --strict`。
- [ ] 已运行 `python -m pytest`。
- [ ] 没有在线调用生图工具或提交个人生成素材。

## 输出合同变化

- [ ] 没有变化。
- [ ] 有变化，并已同步更新 `SKILL.md`、对应 reference、README 和 CHANGELOG。

## 素材授权

- [ ] 新增图片、字体、声音和文本均为自有或已获授权。
