---
name: xuanyituwen
description: 为创作者把一个事件种子编排成六张以上、默认七张的无字伪记录悬疑图组。用户提到伪记录、悬疑图文、恐怖图组、聊天记录式故事、监控式故事、连续图像故事、伏笔、反转、最后一张吓人，或想用 Codex 做一套悬疑图片时，必须使用本 skill。先给三个故事方向，再生成创作者可审的故事控制稿；确认后逐图生成纯净无字底图，并做故事、连续性、竖屏与成片验收。发布页配文由创作者自行填写。
---

# 伪记录悬疑图文编排器

## 运行前提

- 完整图像流程需要宿主提供内置 `image_gen`。
- 图像归一化脚本需要 Python 与 Pillow；本 skill 不在图片内添加文字。

## 目标

把一个模糊的事件种子，变成一套有因果、有信息差、有伏笔回收、视觉连续、可以逐张验收的伪记录悬疑图文。

这个 skill 面向创作者本人。故事控制稿直接给创作者看，不另做一份“观众版故事”。图片只交付纯净无字图，抖音发布页的配文、标题和话题由创作者本人填写；本 skill 不在图片内嵌任何文字。

## 默认规格与可调整项

- 默认七张，最低六张；故事需要时扩展到八张、九张或更多。
- 默认画布为 9∶16，最终导出 1080×1920。
- 图片本身不出现抖音界面、平台水印、头像、用户名、点赞、评论、标题或分享按钮。
- 关键人物和决定性线索避开未来发布时可能被平台界面覆盖的区域。
- 默认视觉预设是真实记录感，像手机随手拍、监控截帧、现场照片或设备记录；视觉预设可以在提案阶段切换。
- 默认异常逐步渗入，最后一张完成恐怖揭示、悬疑揭示或突发惊吓；用户可以在提案阶段改变速度。
- 同组图的角色、场景、关键物件、时间关系和记录来源必须连续。
- 图片内不嵌文字；每张图都必须带来新事实、状态变化、判断变化或新的问题。

## 总流程

严格按以下阶段推进。用户只要故事方向、控制稿或分镜时，停在对应阶段，不调用 `image_gen`。

### 阶段一：接收事件种子

从用户输入中提取：

- 事件种子、人物关系或想营造的感觉。
- 已知地点、时间、道具、记录来源和视觉参考。
- 想要的恐怖强度、结尾类型和是否允许开放解释。
- 是否指定视觉预设、图数、记录载体或最终尺寸。

如果缺少的信息不会改变故事方向，就自己补足。只有在会改变真相、视角、结尾或生成方式时，才提一个高影响问题，不要把所有决策重新推回给创作者。

用户提供的图片如果只是风格参考，就把它标成参考图，不把其中的平台界面当作故事内容；除非用户明确要求编辑原图，否则先生成新的无字底图。

如果参考图只有本地路径而没有出现在当前对话中，先用 `view_image` 检查它，再决定是风格参考还是编辑目标。

### 阶段二：先给三个故事方向

不能直接跳到生图。先给三个明显不同的方向，其中一个标为主推荐。每个方向只展开到足以做选择的程度，包含：

1. 一句话悬念。
2. 记录来源与核心视角。
3. 表面故事。
4. 创作者知道的底层真相，或明确的不可解释规则。
5. 最后一张的落点。
6. 至少三条可回看的伏笔方向。
7. 预计图数和节奏差异。

方向可以混合档案证据、第一视角目击、都市传说、监控、聊天记录、现场照片等组件，不把它们当成互斥题材。让创作者选定一个方向后，再进入下一阶段。

### 阶段三：生成故事控制稿并暂停确认

读取 `references/story-control-sheet.md`，生成完整故事控制稿。控制稿必须给创作者看，并包含：

- 核心问题、核心情绪和故事表面。
- 作者真相、人物动机、人物之间的关系与时间线。
- 故事事实、角色知识、记录者观察权限、作者真相和当前允许揭示边界。
- 角色档案、场景档案、关键物件档案和记录来源档案。
- 场景空间锚点账本：先选场景母版，为关键结构和道具登记唯一锚点 ID、父面、归一化坐标、局部位置、相邻关系、朝向、允许变化和禁止变化。
- 伏笔账本、读者期待账本、行动代价账本和节奏功能表。
- 六张以上的逐图故事骨架。
- 每条伏笔在哪一张埋设、回响、推进、回收或有意延后。
- 最后一张如何重新解释至少两处前文细节，并造成新的现实后果。

控制稿里必须有明确的底层真相，或者明确写出“不可解释”本身就是设定。开放结局可以留白，但不能用开放结局掩盖作者没有想清楚。

等待创作者确认。创作者未确认前，不生成图片。

### 阶段四：拆成逐图卡片

读取 `references/frame-card.md`，为每一张图建立卡片。每张图都要回答：

`证据或压力 → 角色解释 → 行动选择 → 可见后果 → 下一张的新问题`

推荐的默认七张节奏是：

1. 普通记录里出现一个具体问题。
2. 人物、地点和记录来源被定位，给出可验证的局部进展。
3. 异常以背景、道具或人物反应的方式露头。
4. 角色采取一次验证或处理行动，但解释与读者判断开始分叉。
5. 表面解释短暂成立，随后被新的证据反证。
6. 倒数第二张让观众确认异常，回收一部分早期线索。
7. 最后一张重新排列前文意义，产生恐怖、悬疑或突发惊吓，并留下后果。

六张时可以压缩验证与反证；八张以上时可以把人物动机、失败验证或余波拆开。不要让每张图都只是“发现新线索”，要让信息、判断和代价发生变化。

硬性故事门槛：

- 核心真相至少提前埋下三条线索。
- 线索尽量来自不同载体，例如环境、人物行为和物件状态。
- 可以有误导线索，但误导必须能被前文事实支持。
- 最后一张不能凭空增加决定性的新身份、新录像、新能力或新设定。
- 反转后，前面至少两处可见细节获得新的解释。
- 如果混合记录载体，必须写清切换发生的时间、来源、记录者和观看权限。

空间连续性硬门槛：

- 在第一张图生成前，读取 `references/spatial-anchor-ledger.md`，建立一张场景母版和空间锚点账本。
- 每个会影响叙事的结构或道具必须有唯一锚点 ID，至少登记所属父面、归一化包围框、局部位置、两个相邻锚点、朝向和禁止变化项。
- 不能只写“在右边”“靠下”“门旁边”。必须写清它是在门扇、门框、墙面还是其他载体上，并给出相对于相邻结构的关系。
- 每一张逐图卡都要标明本张锚点是否可见、是否被遮挡、状态如何变化；锚点被遮挡不等于可以重新摆放。
- 若场景换角度，保持父子关系和局部坐标，不要求物件永远落在屏幕同一个像素；严禁镜像、换父面、改变上下顺序、改变相邻距离或凭空增加同类物件。

### 阶段五：生成无字底图

默认使用内置 `image_gen`，一张图一个调用。不要为了省步骤把七张不同画面塞进一次生成，也不要让模型负责准确生成中文。

先读取 `references/image-prompt-presets.md` 和 `references/spatial-anchor-ledger.md`。每张实际 prompt 按“全局基底＋一个记录载体＋角色与场景连续性锁定＋空间锚点合同＋本张证据＋物理关系或光源关系＋负面约束”的顺序组装；只有终局图才追加终局惊吓段。不要为每张图临时发明一套新的风格描述。

每张底图提示词必须写清：

- 记录来源、设备感和画面可信度。
- 时间、地点、天气、光线、镜头距离和构图。
- 角色外观、衣着、动作和可识别特征。
- 关键物件的锚点 ID、所属父面、局部归一化位置、朝向、相邻结构关系、状态和与上一张的变化。
- 本张必须出现的视觉证据。
- 如果叙事依赖方向、距离、影子、脚印、反光或光源，写出两个参照物、明确方向和可验证的物理关系。
- 9∶16 竖屏、无平台界面、无水印、无任何文字。
- 手机、监控、门铃或其他设备出现时，追加屏幕负面约束，禁止图标、缩略图、按钮、状态栏和界面布局。

把角色档案、场景档案、物件档案、空间锚点账本和记录来源档案作为连续性真相源，逐张复用。后续画面如果沿用同一场景，优先把场景母版或上一张合格图作为参考图，并同时保留完整的空间锚点合同；参考图不能替代合同。

每张底图生成后立即逐锚点检查：父面、左右关系、上下顺序、局部位置、相邻关系、朝向和尺度是否保持。发现铰链从门框跑到门扇、门链换边、猫眼上下移动、柜体镜像或其他空间锚点漂移，就判定本张失败，只针对该问题重生成或编辑，不把错误带到下一张，也不把失败图当作新的参考图。

内置 `image_gen` 的结果默认可能留在 Codex 生成目录。只要本次任务要交付项目文件，就把选中的底图复制或移动到输出包的 `base/`，不能只留下默认目录路径；不要覆盖已有图片，必要时使用带版本号的文件名。

### 阶段六：交付纯净无字图组

不创建图片内文字规格，不在图片内嵌时间框、对白、字幕、标题或调查备注。抖音发布页配文由创作者自行填写。

将选中的无字底图保存在 `base/`，再用 `scripts/normalize_raster.py` 统一输出到 `final/`。`final/` 仍然是纯净无字 PNG，只负责尺寸、色彩模式和文件格式归一化。

### 阶段七：最终验收

读取 `references/quality-gate.md`，按以下顺序审查：

1. 因果链是否成立。
2. 信息差是否清楚，角色是否只使用自己能获得的知识。
3. 伏笔是否公平，反转是否重排既有细节而不是凭空补设定。
4. 角色、场景、物件、时间和记录来源是否连续。
5. 每张图是否产生推进、变化或新问题。
6. 画面是否符合选定视觉预设，最后一张是否有足够的落点。
7. 每个空间锚点的父面、局部位置、相邻关系、上下顺序、朝向和尺度是否稳定，是否发生镜像或漂移。
8. 是否为纯净无字 9∶16 画面，是否误带平台界面、水印、账号信息或生图模型残留文字。

任一硬门槛失败，就回到对应阶段修正，不用漂亮画面掩盖故事问题。

## 输出包

用户没有指定其他位置时，默认在项目根下创建：

```text
lines/ai-build/assets/pseudo-record/<故事短名>/
├── story-control.md
├── frame-plan.md
├── continuity-ledger.md
├── spatial-anchor-ledger.md
├── prompts/
├── base/
├── final/
└── README.md
```

如果用户只要求方向或控制稿，只输出当前阶段的文档，不提前创建图片目录。完成成片时，报告每个最终图片的绝对路径、使用的视觉预设、图数、无字归一化方式和未通过的检查项。

## 参考资料按需加载

- 故事控制稿：读取 `references/story-control-sheet.md`。
- 逐图拆解：读取 `references/frame-card.md`。
- 场景空间锚定：读取 `references/spatial-anchor-ledger.md`，先建场景母版和锚点合同。
- 视觉预设切换：读取 `references/visual-presets.md`。
- 生图 prompt 组装：读取 `references/image-prompt-presets.md`。
- 最终验收：读取 `references/quality-gate.md`。

现成 GitHub 项目只吸收方法，不复制受许可证约束的原文或文件。故事逻辑优先独立重写，视觉证据连续性、无字成片和抖音发布规则由本项目自建。
